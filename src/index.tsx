const ReactNativeBrotherPrinters = require('./NativeReactNativeBrotherPrinters').default;

export function multiply(a: number, b: number): number {
  return ReactNativeBrotherPrinters.multiply(a, b);
}
